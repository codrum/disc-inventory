import { Typography } from '@mui/material'
import * as React from 'react'

const ServerError = props => {
	return (
		<>
			<Typography>API Is Down</Typography>
		</>
	)
}

export default ServerError
